# REPRO UI LAYOUT

> *Native flex solver vs the boxed arithmetic it replaces.*

Runs the native flex solver (`crates/tish-agb/src/ui_layout.rs`) against the boxed implementation on the same layout and diffs the result.
