{
  "name": "repro-654-sibling-stack",
  "private": true,
  "scripts": {
    "build": "tish build src/main.tish --target gba -o \"$npm_package_name.gba\"",
    "clean": "rm -rf .tish \"$npm_package_name.gba\" \"$npm_package_name.sav\""
  },
  "devDependencies": {
    "@tishlang/tish": "file:../../../../tish/tish/npm/tish"
  },
  "tish": {
    "rustDependencies": {
      "tish_agb": { "path": "../../crates/tish-agb" }
    }
  },
  "version": "0.0.0",
  "description": "Isolate a large SRPG example's pattern: many sibling FunDecls must not Value-extract at every call entry."
}
