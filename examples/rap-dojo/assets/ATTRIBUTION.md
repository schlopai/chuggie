# rap-dojo asset attribution

Every PNG in this directory is generated by
[`scripts/gen_rap_dojo.py`](../../../scripts/gen_rap_dojo.py) (`npm run assets`). Nothing here is
hand-edited — edit the script.

## Source pack

**[Ninja Adventure](https://pixel-boy.itch.io/ninja-adventure-asset-pack)** — CC0 1.0, by
**Pixel-boy** and **AAA**. Vendored at [`assets/ninja-adventure/`](../../../assets/ninja-adventure/);
attribution is appreciated but not required.

| Generated | Source | Notes |
|---|---|---|
| `student.png` | `Actor/CharacterAnimated/NinjaGreen/Separate/{Idle,Attack,Push,Roll,Hit,Item}.png` | 9 poses at 32×32. Source clips are **column = direction, row = frame** and must be transposed. |
| `master.png` | `Actor/Boss/GiantBamboo/{Idle,Walk,Attack,Hit}.png` | 9 poses, 62×62 art centred in 64×64 cells. |
| `icons.png` | `Ui/Input/Gamepad/Button{A,B}/{Idle,Pressed}.png`, plus drawn arrows | See below. |
| `stage.png` | `Backgrounds/Tilesets/TilesetHouse.png` (banners cols 22–28 rows 20–21; DOJO sign col 4 row 4) | Wall, floor and perspective are drawn; the dressing is pack art. |

## Two things that are drawn rather than lifted

**The direction arrows.** The pack's `Ui/Input/Gamepad/Button{Up,Down,Left,Right}.png` are not
arrows — each is a 17×17 picture of the whole d-pad with one circle tinted. That reads at a glance
in a settings screen and reads as four smudges in a 16px icon flying past on the beat.
`Ui/Arrow.png` is closer, but it *looks* like an arrow because of its internal shading; its
silhouette is a rounded blob, so lifting its alpha as a shape yields an octagon. The arrows are
therefore drawn, keeping the pack's own colour coding for each direction (up orange, down green,
left blue, right red).

**The floor.** No pack ships a perspective ground plane, because it is a property of the camera
rather than of the art. See the [README](../README.md) for how it is built and why the tile widths
are 8/16/32/64.

## Fonts

Referenced in place from [`assets/fonts/`](../../../assets/fonts/), never copied:

- **Kenney High Square** (`kenney-high-square.ttf`) — Kenney, free/CC0.
- **tinypixel** (`tinypixel.ttf`) — SIL Open Font License.

## Music

`battle.deck` is generated by
[`scripts/gen_rap_dojo_music.py`](../../../scripts/gen_rap_dojo_music.py) alongside
`src/chart.tish`. Original composition; no sampled audio is used anywhere (deck's `gbaDirectSound`
is a second synth, not a sampler).
