# BENCH SYSTEMS

> *Where the native pass goes, system by system.*

A benchmark ROM that times the engine's native systems one by one, so a regression in any single system shows up as a number rather than a vibe.
